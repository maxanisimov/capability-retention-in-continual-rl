# Safe CRL

Safe continual RL project workspace.

- `pipelines/` contains experiment pipelines and pipeline-local code.
- `utils/` contains project-specific reusable helpers.
- `artifacts/`, `results/`, and `plots/` contain generated or collected outputs.
- `archive/` contains older exploratory material.
