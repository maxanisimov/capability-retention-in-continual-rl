# Safe Policy Optimisation

Scaffold for safe policy optimisation experiments.

- `pipelines/` is for experiment entry points and pipeline-local code.
- `utils/` is for project-specific helpers.
- `artifacts/` is for generated outputs.
